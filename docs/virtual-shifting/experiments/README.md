# Experiment Log — Index

One line per experiment file, in execution order, with the one-line outcome. Full detail
lives in each `NN-short-slug.md` file (template: Date / Hardware & firmware / Hypothesis /
Setup / Exact steps performed / Raw captured data / Observations / Conclusion / Confidence
/ Follow-ups). The live test matrix (question → scenario → decision) is
[00-test-matrix.md](00-test-matrix.md).

| # | File | Experiment | Outcome |
|---|---|---|---|
| 00 | [00-test-matrix.md](00-test-matrix.md) | Phase 0 question harvest + test matrix (not a hardware experiment) | Live doc |
| 01 | [01-dual-connection-smoke-test.md](01-dual-connection-smoke-test.md) | HW-V0 — dual Web Bluetooth connection (trainer + Click) on real hardware | **PASS** — both stayed connected ~76s, no drops. Bonus: trainer's Zwift-service ASYNC channel emits unsolicited debug-log text |
| 02 | [02-firmware-model-check.md](02-firmware-model-check.md) | HW-V1 — trainer + Click firmware/model ID | KICKR CORE C26B fw 1.5.36 (Plan A′ gate open); Click fw 1.2. Bonus: **two** Click units on hand (Left+Right), not one |
| 03 | [03-click-buttons-partial.md](03-click-buttons-partial.md) | HW-V2/V3/V4/V5/V6 — Click button mapping | **PAUSED, partial** (disconnect-cadence root cause). Mapping/architecture parts superseded by 04 |
| 04 | [04-click-mapping-and-relay-confirmed.md](04-click-mapping-and-relay-confirmed.md) | Full Click button mapping + relay architecture | **CONFIRMED.** Right "+"=0x20, Left "−"=0x100, D-pad matches community table. Left/Right pair relays through ONE BLE connection — only need to connect one unit, big adapter simplification |
| 05 | [05-ftms-conformance-hw-v10.md](05-ftms-conformance-hw-v10.md) | HW-V10 — FTMS Control Point conformance (ERG/SIM interleave, concurrent write, Reset) | **ANSWERED**, self-corrected mid-session. Chrome itself blocks concurrent same-characteristic writes client-side. KICKR's ATT response to a post-Reset command was Success (confirmed protocol fact); the follow-on "genuine resistance change" claim was overreach from a single uncontrolled trial — demoted to base validation BV1 |
| 06 | [06-hw-v7-v8-mass-regression.md](06-hw-v7-v8-mass-regression.md) | HW-V7+V8 combined — FTMS latency + trainer-assumed-mass regression | **ANSWERED, high confidence.** Fixed-gear/constant-cadence protocol (developed this session after a confounded first attempt): power-vs-grade R²=0.9999, trainer's assumed mass (93.3kg) within 1.4% of actual (92kg) — substantially de-risks R3. ACK latency 2-5ms |
| 07 | [07-outdoor-ride-power-grade-comparison.md](07-outdoor-ride-power-grade-comparison.md) | Ground-truth cross-check — real outdoor ride power-vs-grade chart vs. HW-V8's trainer regression (not a live BLE experiment) | **Directionally confirming, not a controlled cross-check.** Real 6% grade = 238W (freely geared) vs. HW-V8's fixed-gear 353.8W — 49% higher, strong support that "6% felt like 15%" was a fixed-gear-protocol artifact, not a physics error. Outdoor power saturates ~250-266W from 8% on, independently corroborating the rider's self-reported ~250W ceiling |
| 08 | [08-hw-v12-bakeoff-partial.md](08-hw-v12-bakeoff-partial.md) | HW-V12 — shift-primitive bake-off, **candidate (a) only** (session ended early) | Candidate (a) grade-offset additive: scored **17/20** (works, no dead zone, but arbitrary/not speed-scaled). Feature-gate check confirms candidate (e) is viable. Candidate (b)'s grades pre-computed for next session using this session's own measured baseline (found the design's default r_phys=2.40 assumption doesn't match this rider's actual gear — real r_phys≈1.85). Candidates (c)-(f) not yet started |
| 09 | [09-outdoor-stream-physics-regression.md](09-outdoor-stream-physics-regression.md) | Per-second outdoor stream physics fit via an intervals.icu Custom Activity Chart script (naive regression + flat-segment sweep + Chung virtual-elevation grid search), vs. HW-V8's trainer constants (not a live BLE experiment) | **Multi-ride result, DESIGN §4.9 answered — currently negative.** [`intervals-icu-power-model-chart.js`](intervals-icu-power-model-chart.js) (chart) + [`intervals-icu-calibration-field.js`](intervals-icu-calibration-field.js) (companion Computed Activity Field). 3 rides' coarse-grid climb-only R²/MAE: HW-V8 trainer constants beat the outdoor-fitted model on every ride. Cw landed on the identical grid point (0.30) across all 3 — flagged as likely under-resolution, not convergence; superseded by 10 |
| 10 | [10-offline-fit-physics-analysis.md](10-offline-fit-physics-analysis.md) | Offline, full-precision re-fit of 3 real FIT files (fitparse/numpy/scipy; Method C as continuous joint optimization, no downsampling/grid) — replaces the intervals.icu sandbox for this question (not a live BLE experiment) | **Both open confounds from 09 substantially disentangled; headline verdict unchanged (still negative).** Removing the grid did NOT converge Cw (spread 0.050-0.436, wider than before); 2/3 rides' mass hit a genuine (verified, not artifact) optimizer boundary. Root cause traced analytically: a mass→∞ degenerate direction lets the fit substitute acceleration for power. Climb-only breakout under both steady-state AND acceleration-inclusive formulas: trainer constants win on every ride, both formulas — refutes the fit-objective/eval-metric-mismatch hypothesis as sufficient on its own |
| 11 | [11-capture-backend-selection.md](11-capture-backend-selection.md) | Phase 0 of the connection-sniffing effort: capture-backend ranking + the `tools/ble-lab/` toolchain (environment probe, not a BLE hardware run) | **Phase 0 ANSWERED; Phases 2-3 BLOCKED.** Ranking revised — **Android HCI snoop promoted above macOS PacketLogger** (host-side taps above the link layer ⇒ plaintext ATT either way, but PacketLogger + Zwift for Mac are both absent and login-walled, while `adb` is installed and Zwift Companion is already **proven** to connect/unlock our Clicks). OTA sniffer: **do not buy _yet_** (nRF52840 dongle £10.45 verified — revised in the addendum: it is the only backend that captures *both* endpoints from one point, so worth buying if the free `FF 04 00` test fails and host-side setup proves awkward). Addendum also assesses a **Linux home server**: it *cannot* sniff the app↔Click link (host-side HCI logs only see their own controller; normal BLE chips have no promiscuous mode and cannot follow a connection's channel hopping) but is valuable as an **unrestricted-BLE-client oracle** — and exposed a latent bug, since `btmon -w` writes btsnoop datalink 2001 which our parser rejected, so `analyze.py`/`diff.py` now hand files to tshark directly. **FINAL (4th addendum): macOS PacketLogger EMPIRICALLY FAILS on this Mac** — attaches to the HCI source and receives zero packets, confirmed with every confound removed (free tap, no pipe buffering, Bluetooth off/on to guarantee traffic, SIGINT stop): 260 bytes, one `Disconnected from OS X Device` note, no packets. Leading cause: the missing `PacketLoggerHelper` privileged helper (root alone is insufficient). **Recommendation flipped to BUY the nRF52840 dongle** (£10.45) — the only backend that captures both endpoints from one point; tshark's `nordic_ble`/`btle` dissectors + `WTAP_ENCAP_NORDIC_BLE` verified present *before* recommending the spend, and `nordic_ble.direction` wired in (polarity still unverified). Built + verified `tools/ble-lab/` (5 CLIs + Web Bluetooth replay harness, 65/65 self-test, no hardware needed) |
| 12 | [12-connection-capture-preregistration.md](12-connection-capture-preregistration.md) | Pre-registered predictions for all 9 Phase-2 capture scenarios, with execution order (not yet run) | **Pre-registration only — nothing run.** Reorders the mission's list to put `trainer-control` **first** as the method's control experiment (our FTMS path demonstrably works, so any divergence there is a false positive in the tool). Names the 4 predicted app-vs-our-harness divergences in advance from a code read, so the centrepiece diff is a test rather than a fishing trip |
| 13 | [13-ff-family-frame-decode.md](13-ff-family-frame-decode.md) | Offline decode of the three `0xFF`-family frames that `03`/`04` recorded but explicitly left undecoded — targets the authcode/keep-awake question directly (no new hardware run) | **H26 CONFIRMED for structure; reframes the authcode question.** All three are `FF <sub> 00` + protobuf. The 85-byte frame `04` called "cryptographically random" carries a **33-byte compressed P-256 public key** (`0x03` prefix), a `02 03 00 00` version field, and a 40-byte (32+8) blob — and it arrives **unsolicited on a plain Web Bluetooth connection**. `FF05` frames carry the ASCII serial `34C4593D51A6` plus a candidate countdown (496). **Actionable**: `PROTOCOLS.md` §1.5's `FF 04 00` is an *empty-bodied* assertion we have never sent — testable in a browser in minutes, no capture needed |
| 14 | [14-clickv2-unlock-current-state.md](14-clickv2-unlock-current-state.md) | Current (2026) state of the Click V2 unlock; source-currency audit (literature only, no hardware) | **H27 FALSE — three of our four external sources pre-date the device we own.** ajchellew/zwiftplay dead since 2024-02-11 (its Click support is V1); both makinolo posts describe Ride/Play. Only OpenBikeControl/bikecontrol is live (6.3, 2026-07-10). **Corrects `13` §3**: Click V2 does NOT have encryption removed — it has an authorisation context that expires, matching our 44-90s drops, so **H16 is now externally corroborated**. **The authcode CANNOT be reproduced**: unlock needs the real Zwift app + Zwift servers (not Companion), and BikeControl keeps that implementation proprietary (public repo ships a stub). **Two actionable finds**: unlock characteristics `00000100-`/`00000101-19ca-…` that our docs and code have never touched; and BikeControl's own UI states **the RIGHT-side Click V2 needs no unlock at all** — which may make H16 avoidable rather than solvable. Also flags a conflict: BikeControl says *"Open Zwift (not the Companion)"* whereas our `03`/`GOALS.md` credit Companion |
| 15 | [15-zwift-app-click-session.md](15-zwift-app-click-session.md) | First capture of a real Zwift client connecting a Click V2 — Zwift **Companion** (not the game), via Android HCI snoop, cross-correlated with the phone's own logcat | **Attribute table CONFIRMED; handshake still uncaptured; and a mid-session self-correction.** Full Click V2 table read off Companion's own discovery, incl. `0100`/`0101`/`0102` (properties + CCCDs + three unread `2901` User Descriptions). Companion subscribed to six CCCDs, read `0004` → **zero-length**, and wrote **no payload anywhere** before the link died at **73.5 s** (HCI `0x08`, then 12 failed reconnects). ⚠️ **Corrected after user challenge**: the peer was `com.zwift.android.prod` = **Companion**, not the game — so this does **NOT** discriminate H16 (authorisation) from **H28** (idle timeout), and an intermediate claim that it refuted H16 is retracted. Logcat correlation also found the phone's **30 s screen timeout firing mid-session**; doze largely exonerated because the trainer's idle link survived the same window 2 min 15 s. Identifies **which physical unit is which** via the `0xC0` static-random-address rule — our harness has been on the *other* Click all along. **§6.0 pre-registers the capture that should get the unlock**: Companion in **bridge** mode with the game on a Mac, which keeps an authorised session's BLE on the phone where our route is proven (5 predictions). Found and fixed **4 silent `tools/ble-lab` defects** (lost ATT payloads, cross-device UUID leakage, a lossy address filter, HCI events excluded so the whole connection lifecycle was invisible) and added the Android pipeline (`android-capture.py`, logcat→markers, measured clock-skew correction); self-test 70/70 → **111/111**. Overturns `11`'s "tshark does all ATT dissection" decision |
| 16 | [16-bridged-zwift-session-capture.md](16-bridged-zwift-session-capture.md) | **The bridged Zwift session** — game on the Mac, bridged via Companion on the phone, which held BLE to the trainer and BOTH Click units. Two simultaneous taps: Android HCI snoop + `tcpdump` on the Mac | **The capture that worked, and it moved the project.** 4 of 5 pre-registered predictions confirmed, 1 falsified. **H28 (idle timeout) FALSIFIED**: the authorised link sat **225.8 s with zero ATT traffic** and never dropped (still up at +310 s); no supervision timeout anywhere in the session. **New H29 — the handshake is the keep-alive**: in the same file, the link that never sent `RideOn` died at 73.5 s, the one that sent `RideOn 02 03` survived 226 s idle. **PROTOCOLS §1.3 + H15 corrected — Zwift writes `RideOn 02 03` (8 bytes), not the bare 6 we send**; Click echoes `02 03`, trainer answers `02 02` (independently confirming the Feb-2026 prototype log). **A 2-byte change to our harness is now the highest-value test in the project.** Also: **no payload write to `0100`/`0101`/`0102`** even when fully authorised (P1 falsified, `0102` silent); **`13`'s `FF 04 00` prediction CONFIRMED** (sent 0.43 s after the device's `FF 03` P-256 challenge); **`41 08 05` confirmed as real Zwift** (U14, not a QZ invention); **FTMS Control Point never written** — control via the Zwift hub service + Wahoo `a026e037` (bears on U9); both Clicks connected simultaneously with different sequences. **LAN bridge measured**: TCP port 21588, 4-byte length framing, **encrypted** (entropy 7.74/7.91) — no plaintext authcode, though the pcap misses the pairing moment. Corrects `11`'s "rolling ~7-minute buffer" — this file spans 127.6 min |
| 17 | [17-crr-cw-sweep-protocol.md](17-crr-cw-sweep-protocol.md) | **H31 — does the KICKR honour the FTMS `Crr`/`Cw` bytes, or only the grade?** Pre-registered protocol: 6 laps of a synthetic constant-grade route with tyre/position swept A-B-A-C-D-A (not yet ridden) | **RIDDEN 2026-08-05 — H31 partially answered. Crr IS honoured** (speed-matched, positive in every bin, mean +74 W vs +88 W predicted = 84%); **Cw not established** (5-47% of prediction, below the scatter). **P4 confirmed** — sent grade in the 0% blocks was condition-invariant to within 0.10 pp across a full sweep, so the trainer saw the same grade in every condition and the design's single-causal-pathway claim held. Two corrections fell out: predictions about the trainer must use the **trainer's own speed** (5.1-6.8 m/s), not `v_virt` (7.13) — worth 53%→84% on its own — and **absolute** power is still unpredicted (MAE ~120 W, negative R²), so only differences-in-differences are sound; an early pass using absolute residuals reached the opposite Crr conclusion. FIT cross-validated against the browser's own IBD lines to **0.2 W** on mean pedalling power. **Two bugs**: the ride log recorded `power: 0` for every sample of every run (`data.power` vs `powerW`) — the FIT is the only reason the ride is analysable, now fixed; and the **Zwift Click disconnects when a workout starts** (its panel owns the GATT link and unmounts), so paddles are dead mid-ride and only `[`/`]` shift — not yet fixed. Pre-registration: Rests on a *measured* property of our own model: in the baseline gear the sent grade is invariant to Crr and Cw (**0.019 pp** for the full 0.004→0.020 Crr sweep, **0.069 pp** for 0.51→0.20 Cw — both inside `setSimGrade`'s 0.3% deadband), so the trainer is the *only* remaining causal pathway and the test is decisive either way. Off baseline the same sweep moves the sent grade by up to **1.9 pp** and **flips sign** either side of baseline, which is why gear and cadence are pinned on the 0% blocks — that confound correlates perfectly with the swept variable, so it biases rather than averages out. Predicted Δpower if honoured: **≈109 W** (Crr) and **≈112 W** (Cw) at the rider's actual 97 kg, vs ±10–15 W rider noise. Ships [`17-crr-cw-sweep-route.json`](17-crr-cw-sweep-route.json) + [`make_sweep_route.py`](make_sweep_route.py) (1.70 km, plateaus at 0/3/6/−2/0%, verified through the app's own import path) and forced two recorder fixes: the ride log now carries per-sample `crr`/`cw` (they existed only in the session header, so a mid-ride sweep would have been unattributable on export) a `physicsApplied` note marking each condition change, and — after the rider pointed out they would ride it as stop / adjust / start again — `startRideLog` now **archives** a completed run instead of clearing it, since every Start Workout press was silently destroying the previous lap |
